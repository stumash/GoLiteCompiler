package main 

//Valid SPECIAL
//Here it knows that the function will go into for at least once, so it does not check for the entire function if it has a return 


func f ( ) int {
	for {
		return 0
	}
}